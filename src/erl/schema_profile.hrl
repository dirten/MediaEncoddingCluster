-record(
  profile,
  {
    id=-1,
    name,
    ext,
    vformat,
    vcodec,
    vbitrate,
    vframerate,
    vwidth,
    vheight,
    achannels,
    acodec,
    abitrate,
    asamplerate,
    multipass=0,
    gop=20
  }
).
